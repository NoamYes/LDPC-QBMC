%% the function gets the Matrix and a var vector full of q-1 values, and log2(q)
%  the function returns the check2var calculation

function [var] = var2check(H,c2v, v2c_s, c2v_s)

    tmp = c2v(v2c_s.rows);

end
